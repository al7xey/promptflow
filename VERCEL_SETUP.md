# Настройка Vercel для работы с GigaChat API

## ✅ Что уже исправлено в коде:

1. ✅ `rejectUnauthorized: false` — всегда для работы с GigaChat (исправлена критическая ошибка)
2. ✅ Используется Node.js runtime (не Edge)
3. ✅ Запросы идут через API route (serverless function)

## 🔧 Что нужно настроить в Vercel:

### 1. Добавить переменные окружения

**Vercel Dashboard → Ваш проект → Settings → Environment Variables**

Добавьте:
```
GIGACHAT_AUTH=ваш_base64_encoded_credentials
```

Где `GIGACHAT_AUTH` — это Base64-кодированная строка `client_id:client_secret`

**Важно:**
- Тип окружения: **Production** (и Preview, если нужно тестировать)
- После добавления → **Redeploy** проект

### 2. Проверить Runtime

По умолчанию Next.js API routes используют Node.js runtime — это правильно.

❌ **НЕ используйте Edge Runtime** — там нельзя отключить проверку TLS-сертификата.

### 3. Передеплой

После добавления переменных окружения:
1. Перейдите в **Deployments**
2. Нажмите на последний деплой
3. Нажмите **Redeploy**

## 📝 Примечания

- Таймаут на бесплатном плане Vercel: ~10 секунд
- Токен GigaChat кешируется на 30 минут (в памяти функции)
- API работает только на сервере (не из браузера)

## 🔍 Проверка

После деплоя проверьте:
1. Логи в Vercel Dashboard → Functions → `/api/generate`
2. Если ошибка 500 — смотрите логи для деталей
3. Убедитесь, что переменная `GIGACHAT_AUTH` установлена

