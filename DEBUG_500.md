# Диагностика ошибки 500 на Vercel

## 🔍 Как проверить логи в Vercel:

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Deployments** → выберите последний деплой
4. Нажмите **Functions** → найдите `/api/generate`
5. Откройте **Logs** — там будет детальная информация об ошибке

## ✅ Что уже исправлено в коде:

1. ✅ `rejectUnauthorized: false` — для работы с GigaChat
2. ✅ `export const runtime = "nodejs"` — явно указан Node.js runtime
3. ✅ Проверка переменной окружения `GIGACHAT_AUTH`
4. ✅ Улучшенное логирование ошибок

## 🔧 Что проверить в Vercel:

### 1. Переменные окружения
**Settings → Environment Variables**

Убедитесь, что добавлено:
- `GIGACHAT_AUTH` = ваш Base64-encoded credentials
- Тип: **Production** (и Preview если нужно)

### 2. Передеплой
После добавления/изменения переменных:
- **Deployments** → последний деплой → **Redeploy**

### 3. Проверка логов
Смотрите логи функции `/api/generate` — там будет точная ошибка:
- Если "GIGACHAT_AUTH не установлена" → добавьте переменную
- Если ошибка SSL → уже исправлено в коде
- Если ошибка авторизации → проверьте правильность credentials
- Если таймаут → запрос слишком долгий (лимит 10 сек на бесплатном плане)

## 📝 Типичные ошибки:

| Ошибка | Решение |
|--------|---------|
| `GIGACHAT_AUTH не настроена` | Добавьте переменную окружения в Vercel |
| `HTTP 401` | Неправильные credentials или токен истек |
| `HTTP 500` от GigaChat | Проверьте логи Vercel для деталей |
| Таймаут | Запрос > 10 секунд (бесплатный план) |

## 🚀 После исправлений:

1. Запушьте изменения в GitHub
2. Vercel автоматически задеплоит (или сделайте Redeploy)
3. Проверьте логи снова

