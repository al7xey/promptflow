# PromptFlow

Современное одностраничное приложение для превращения коротких запросов в глубокие, структурированные промпты для нейросетей.

## Запуск

### Локально

```bash
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Деплой

**Важно:** Это приложение использует Next.js API routes, которые требуют серверной платформы. GitHub Pages не поддерживает API routes (это вызывает ошибку 405).

Рекомендуемые платформы для деплоя:
- **Vercel** (рекомендуется) - автоматический деплой через интеграцию с GitHub
- **Netlify** - также поддерживает Next.js API routes
- **Railway** или **Render** - альтернативные варианты

#### Деплой на Vercel

1. Подключите репозиторий к Vercel через [vercel.com](https://vercel.com)
2. Добавьте переменную окружения `GIGACHAT_AUTH` в настройках проекта
3. Деплой произойдет автоматически при каждом push в `main`

## Особенности

- 🎨 Современный UI в стиле Vercel/Raycast
- 🚀 Использование GigaChat API для генерации промптов
- 📝 Структурированный вывод по методу RTF (Role, Task, Format)
- 📋 Копирование результата одним кликом
- ⚡ Обработка ошибок и loading states
- 🌙 Темная тема с глубоким черным фоном

